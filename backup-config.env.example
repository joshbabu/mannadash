# Copy this to /root/backup-config.env on the server and fill in real values.
# Sourced by backup-db.sh before running.

export R2_BUCKET_NAME=mannadash-backups
export R2_ENDPOINT=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com
