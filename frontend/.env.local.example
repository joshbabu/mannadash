# Copy this file to .env.local to point this app at your local backend instead of production.
# .env.local is gitignored by Vite's default template, so it won't get committed by accident.
VITE_API_BASE=http://localhost:3000
